# Jekyll configuration for GitHub Pages

title: Physical AI SIG
description: OSRA TGC Special Interest Group for Physical AI

# Theme for rendering markdown to HTML
theme: jekyll-theme-dinky

# Site settings
url: "https://ros-physical-ai.github.io"
baseurl: ""

# GitHub repository metadata (for jekyll-github-metadata plugin)
# For local testing: use your fork's repository name
# For production: change to "ros-physical-ai/.github" when merged upstream
repository: "Juliaj/pai-github"

# GitHub Pages settings
plugins:
  - jekyll-feed
  - jekyll-sitemap

# Sass configuration to suppress deprecation warnings from theme
sass:
  style: compressed
  quiet_deps: true

# Exclude files from processing
exclude:
  - Gemfile
  - Gemfile.lock
  - node_modules
  - vendor
  - .gitignore
  - .pre-commit-config.yaml
  - BUILD.md
  - README.md
  - LICENSE
  - supporting_doc_dont_merge
  - dont_commit
